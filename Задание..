1.parseInt('1011',2)

2.(11).метод tostring(2).padStart(8, '0')

3.константный even2 = testedValue => testedValue | 0b11111110;
критерии: эта функция возвращает для chet чисел свою собственную маску

4. Мы вводим н
Выделяем этот немного широкая маски. Маска = 2 в^П код двоичном

сопи с 3 = (testedValue) => testedValue & 2**н 2
Если бит установлен, то возвращает 2^н иначе 0

5. Массив.из({длина:34}, (В,Я)=>меня).карту( (х,я)=> ( {А, В: С 3(х,3)}) )

6. Сдвинуть на 1 бит влево

7. исключающее ИЛИ
